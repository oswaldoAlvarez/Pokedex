import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imgPosition: {
    position: 'absolute',
    top: -100,
    right: -100,
  },
  flatlist: {
    paddingTop: 20,
    marginLeft: 20,
  },
  mb: {
    marginBottom: 30,
  },
});
